{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'Serving prebuilt dist/ — built locally via: npm run build:hub'",
  "installCommand": "echo 'No install needed — static prebuilt site.'",
  "outputDirectory": "dist"
}
