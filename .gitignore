# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.dist-staging
dist-ssr
*.local

# Local SQLite responses DB (v4) — runtime data, never commit
data/

# dist/ is intentionally COMMITTED: the hub is built locally (where every version branch
# exists) and Vercel just serves the finished files — no branch-fetching during deploy.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel CLI local link/config
.vercel

# Private working notes — never shipped or shared
DEV_NOTES.md
